export const WHATSAPP_NUMBER = '5581999999999' // Substitua pelo número do restaurante, incluindo DDI e DDD, só dígitos

// Marca do restaurante (customizável por restaurante)
export const BRAND = {
	name: 'Restaurante Elegante',
	shortName: 'R', // Usado no círculo do logo
}

// Tema de cores (pode ser alterado por restaurante sem mexer em código Tailwind)
export const THEME = {
	primary: '#8b1d1d', // vermelho profundo
	gold: '#f59e0b', // dourado/âmbar
	pageBg: '#ffffff', // fundo da página (branco por padrão)
	mutedBg: '#fff8e6', // fundo sutil para hovers/realces leves
	mutedBgHover: '#ffefcc', // hover do fundo sutil
	mutedBorder: '#fbd38d', // borda suave (âmbar claro)
}
